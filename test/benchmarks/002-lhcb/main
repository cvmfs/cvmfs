#!/bin/bash

cvmfs_test_name="LHCB benchmark"
cvmfs_test_autofs_on_startup=false
cvmfs_benchmark="yes"

FQRN=lhcb.cern.ch


cvmfs_run_benchmark() {
  # TODO(janpriessnitz): Temporary disabled since it needs fixing
  # set -e
  # source /cvmfs/lhcb.cern.ch/etc/login.sh -c x86_64-slc6-gcc49-opt
  # . SetupProject.sh Brunel v50r3 --use PRConfig
  # gaudirun.py $test_location/PRTEST-COLLISION10-1evts.py
  return 0
}

cvmfs_run_test() {
  # TODO(janpriessnitz): Temporary disabled since it needs fixing
  # logfile=$1
  # test_location=$2

  # run_benchmark
  # local return_code=$?

  # return $return_code
  return 0
}
