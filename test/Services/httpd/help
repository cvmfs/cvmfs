Short:  httpd		Runs a basic webserver on port 8080.

httpd:
httpd:httpd
httpd:	Runs a basic webserver on port 8080. 
httpd:	Usage: httpd <options>
httpd:
httpd:	List of available options:
httpd:	  --port PORT	Listen on port PORT. 8080 by default.
httpd:	  --root DIR	Serve directory DIR. "/tmp" by default.
httpd:	  --stdout FILE	Send output to FILE.
httpd:			Default to /var/log/cvmfs-test/httpd.out.
httpd:	  --stderr FILE	Send error to FILE.
httpd:			Default to /var/log/cvmfs-test/httpd.err.
httpd:	  --index-of	Show and index if no index.html file is found.
httpd:	  --all		Serve any kind of file.
httpd:
httpd:	Error conditions options:
httpd:	  --404		Will always serve 404 error pages.
httpd:	  --403		Will always serve 403 error pages.
httpd:	  --deliver-crap
httpd:			Will always serve the file /tmp/cvmfs.faulty.
httpd:	  --timeout	Wait 300 seconds.
httpd:
